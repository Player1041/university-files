#pragma once
class Snake
{
	 char NAME;
	 int LENGTH;
	 int INITIAL_LENGTH;

	 int getLength();
	 int setLength(int newLength);
	 int addLength(int newLength);

	 int getInitialLength();
	 int setInitialLength();
};

